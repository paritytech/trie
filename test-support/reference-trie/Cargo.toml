[package]
name = "reference-trie"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Simple reference trie format"
repository = "https://github.com/paritytech/trie/"
license = "Apache-2.0"

[dependencies]
hash-db = { path = "../../hash-db" , version = "0.9.0"}
hash256-std-hasher = { path = "../../hash256-std-hasher", version = "0.9.0" }
keccak-hasher = { path = "../keccak-hasher", version = "0.2.0" }
trie-db = { path = "../../trie-db", version = "0.9.0"}
trie-root = { path = "../../trie-root", version = "0.9.0" }
parity-codec = { version = "2.1" }
parity-codec-derive = { version = "2.1" }

[dev-dependencies]
trie-bench = { path = "../trie-bench", version = "0.9.0" }
criterion = "0.1.2"

[[bench]]
name = "bench"
harness = false
